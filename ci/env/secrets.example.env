# TEMPLATE ONLY - never commit real values.
# Make one copy per environment (staging-secrets.env, prod-secrets.env),
# fill it in, and upload each to Jenkins as a "Secret file" credential:
#   ontrack-staging-secrets  and  ontrack-prod-secrets
#
# Generate values with:   openssl rand -hex 32   (keys)
#                         openssl rand -hex 16   (32-char encryption keys)
DB_PASSWORD=
DB_ROOT_PASSWORD=
DF_SECRET_KEY_BASE=
DF_SECRET_KEY_ATTR=
DF_SECRET_KEY_DEVISE=
DF_SECRET_KEY_AAF=
DF_ENCRYPTION_PRIMARY_KEY=
DF_ENCRYPTION_DETERMINISTIC_KEY=
DF_ENCRYPTION_KEY_DERIVATION_SALT=
